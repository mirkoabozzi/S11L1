const initalState = {
  preferiti: [],
};

const mainReducer = (state = initalState, action) => {
  switch (action.type) {
    case value:
      break;

    default:
      break;
  }
};
export default mainReducer;
